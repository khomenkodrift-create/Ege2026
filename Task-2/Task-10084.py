from itertools import product, permutations

    return (x and not y) or (y == z) or not w

        (x1, x2, 0, 0, 0),
        (1, 0, x3, 0, 0),
        (1, 0, 1, x4, 0)
                print(*p, sep='')